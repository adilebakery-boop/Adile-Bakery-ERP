const { ROLES } = require('../constants/roles');
const { PERMISSIONS } = require('../constants/permissions');

const MANAGER_PERMISSIONS = [
  PERMISSIONS.DASHBOARD_VIEW,
  PERMISSIONS.USERS_CREATE,
  PERMISSIONS.USERS_UPDATE,
  PERMISSIONS.USERS_DELETE,
  PERMISSIONS.USERS_VIEW,
  PERMISSIONS.USERS_BLOCK,
  PERMISSIONS.PROFILE_UPDATE,
  PERMISSIONS.PROFILE_PASSWORD_CHANGE,
  PERMISSIONS.PRODUCTS_CREATE,
  PERMISSIONS.PRODUCTS_UPDATE,
  PERMISSIONS.PRODUCTS_DELETE,
  PERMISSIONS.PRODUCTS_VIEW,
  PERMISSIONS.PRODUCTS_MANAGE,
  PERMISSIONS.BRANCHES_CREATE,
  PERMISSIONS.BRANCHES_UPDATE,
  PERMISSIONS.BRANCHES_DELETE,
  PERMISSIONS.BRANCHES_VIEW,
  PERMISSIONS.BRANCHES_MANAGE,
  PERMISSIONS.PRODUCTION_CREATE,
  PERMISSIONS.PRODUCTION_UPDATE,
  PERMISSIONS.PRODUCTION_DELETE,
  PERMISSIONS.PRODUCTION_VIEW,
  PERMISSIONS.REMAINING_CREATE,
  PERMISSIONS.REMAINING_UPDATE,
  PERMISSIONS.REMAINING_DELETE,
  PERMISSIONS.REMAINING_VIEW,
  PERMISSIONS.REPORTS_VIEW,
  PERMISSIONS.REPORTS_EXPORT,
  PERMISSIONS.CATEGORY_ACCESS
];

const STAFF_PERMISSIONS = [
  PERMISSIONS.DASHBOARD_VIEW,
  PERMISSIONS.PROFILE_UPDATE,
  PERMISSIONS.PROFILE_PASSWORD_CHANGE,
  PERMISSIONS.PRODUCTION_CREATE,
  PERMISSIONS.PRODUCTION_UPDATE,
  PERMISSIONS.PRODUCTION_VIEW,
  PERMISSIONS.REMAINING_CREATE,
  PERMISSIONS.REMAINING_UPDATE,
  PERMISSIONS.REMAINING_VIEW,
  PERMISSIONS.CATEGORY_ACCESS
];

const ROLE_PERMISSIONS = {
  [ROLES.ADMIN]: MANAGER_PERMISSIONS,
  [ROLES.MANAGER]: MANAGER_PERMISSIONS,
  [ROLES.BAKER]: STAFF_PERMISSIONS,
  [ROLES.CAKE_CHEF]: STAFF_PERMISSIONS,
  [ROLES.COOKIE_BAKER]: STAFF_PERMISSIONS,
  [ROLES.FETIR_CHEF]: STAFF_PERMISSIONS,
  [ROLES.CASHIER]: STAFF_PERMISSIONS
};

const getPermissionsForRole = (role) => {
  return ROLE_PERMISSIONS[role] || [];
};

const hasRolePermission = (role, requiredPermission) => {
  const permissions = ROLE_PERMISSIONS[role] || [];
  return permissions.includes(requiredPermission);
};

module.exports = {
  ROLE_PERMISSIONS,
  getPermissionsForRole,
  hasRolePermission
};